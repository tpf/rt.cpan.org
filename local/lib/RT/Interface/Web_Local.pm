package RT::Interface::Web;
no warnings 'redefine';

# Stub out the Web* config validation since it doesn't get our reverse proxy setup
sub ValidateWebConfig {}

"Hackity hack hack / don't talk back.";
